// PostCSS configuration file
module.exports = {
  // Define the plugins to be used in the PostCSS processing pipeline
  plugins: {
    // Include Tailwind CSS for utility-first CSS framework
    tailwindcss: {},
    // Add autoprefixer to automatically add vendor prefixes to CSS rules
    autoprefixer: {},
  },
}
